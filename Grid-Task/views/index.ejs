<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Grid Task</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.1.3/dist/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.3/font/bootstrap-icons.css">
    <style>
       
          body{
            background: #f3f3f3;
          }
          button{
            background-color: white;
            padding: 10px;
            border: none;
            box-shadow: 2px 2px 5px;
            border-radius: 10%;
          }
          button:hover, i:hover{
            color: green;
            cursor: pointer;
        }
        .hidden{
          display: none;
        }
        .update-btn{
          background-color: green;
          padding: 7px;
          color: white
        }
        .update-btn:hover{
          background-color: white;
          color: black
        }
    </style>
</head>
<body>
    <table class="table table-hover">
        <thead>
          <tr>
            <th scope="col">#</th>
            <th scope="col">Name</th>
            <th scope="col">Email</th>
            <th scope="col">Contact No</th>
            <th scope="col">Gender</th>
            <th scope="col">Interest</th>
            <th scope="col">Action</th>
          </tr>
        </thead>
        <tbody id="tbody">
            <% for(const data of ans) {%> 
                <tr>
                    <th scope="row"> <%= data.id %></th>
                    <input type="hidden" disabled="true" value="<%= data.id %>" />
                    <td> <input type="text" disabled="true" name="name<%= data.id %>" id="" value="<%= data.name %>"></td>
                    <td> <input type="text" disabled="true" name="email<%= data.id %>" id="" value="<%= data.email %>"</td>
                    <td> <input type="text" disabled="true" name="contact_no<%= data.id %>" id="" value="<%= data.contact_no %>"</td>
                    <td> <input type="text" disabled="true" name="gender<%= data.id %>" id="" value="<%= data.gender %>"</td>
                    <td> <input type="text" disabled="true" name="interest<%= data.id %>" id="" value="<%= data.interest %>"</td>
                    <td> <i class="bi bi-pencil-square edit-btn"></i> </td>
                    <td> <i class="bi bi-check2 hidden update-btn"></i> </td>
                  </tr>

            <% } %>
        </tbody>
      </table>
      <center>
        <button onclick="addRow()"><i class="bi bi-plus-circle"></i> Add New Record</button>
        <button id="saveAll"><i class="bi bi-database-add"></i> Insert All Records</button>
        <button onclick="saveAll()"><i class="bi bi-database-fill-check"></i> Update All</button>
      </center>
</body>
<script src="app.js"></script>
</html>